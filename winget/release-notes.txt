v0.4.0
Moves the app to .NET 10, the current long-term-support release, which also makes the download smaller. Fixes the window layout at display scaling above 100%, where buttons and labels could previously be clipped or overlap.
